import SingleProjectPage, { generateMetadata as gMeta, generateStaticParams as gParams } from '../../projects/[slug]/page';

export const generateMetadata = gMeta;
export const generateStaticParams = gParams;
export default SingleProjectPage;
